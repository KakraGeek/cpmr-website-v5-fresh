# Architect gate log (validation)

Human- and agent-readable record of architect gate outcomes. Authoritative automation mirror: `automation/architect_gates.yml`.

| Date | Story | Outcome | Doc refs | Notes |
| --- | --- | --- | --- | --- |
| 2026-05-15 | E5-S01 — Departments index | **PASS** | 04_architecture §5–§6, 05 §4.3, ADR-003, UX §10.4 | New `departments` collection; `index_shell` for `/departments/` copy; public listing requires `approved` + `verified`. Follow-ons: document extensions in 05 §4.3; E5-S02 landings before promoting card links in global nav. |
| 2026-05-15 | E5-S09 — Department relationship QA | **PASS** | 04_architecture §5.4, 05 §2.5/§19, 06_project_context, REM-CARCH-006 | Fail/warn matrix in `refs.ts`; department FK walk at `content.config.ts` load; id↔stem hard-fail; scholarly FKs warn. Follow-ons: stage `scripts/`; fix or drop §5.4 optional Node CLI; migrate indexes to `getCollection` when `staff`/`services` collections land; tighten scholarly→fail after bulk import per remediation plan. |
| 2026-05-15 | E6-S02 — Research projects index and detail | **PASS** | 04_architecture §5–§6, 05 §4.5, 02_prd §11 R2, REM-CARCH-006 | New `projects` collection with `index_shell` + `project` discriminant; routes `/research/projects/` and `/research/projects/[id]/`; PRD R2 narrative fields in Zod; `department_ids` FK hard-checked against departments; `staff_lead_ids` / `publication_ids` scholarly warn; public surface gated on `editorial_status: approved`; department links only for verified departments. Follow-ons: E5-S05 filter by `department_ids` (not legacy `departmentId`); E6-S04 reciprocal `project_ids`; staff/publication detail links when collections ship; optional migrate FK indexer to `getCollection`; revert story YAML acceptance_criteria wording drift before VERIFIED. |
| 2026-05-15 | E5-S04 — Department staff listing | **PASS** | 04_architecture §5–§6, 05 §4.4, 05 §5.3–§5.5, E6-S03 (`/research/researchers/[id]/`) | `/departments/[slug]/staff/` via `getStaticPaths` + `filterDepartmentStaff` on `department_ids`; public slice uses `isPublicDepartment` + `isPublicStaff`; cards link canonical researcher profiles (no department duplicate detail routes); `sortPublicStaff` ordering; inline client filters only. Follow-ons: resolve `head_of_department_staff_id` on department home hero; align 05 §5.5 canonical path text with `/research/researchers/`; optional shared listing-helper extraction with later E5 child routes. |
| 2026-05-15 | E5-S06 — Department publications listing | **PASS** | 04_architecture §5–§6, 05 §4.6, 05 §5.5, 05 §6.1–§6.3, E6-S04 (`/research/publications/[id]/`), ADR-003 | `/departments/[slug]/publications/` via `getStaticPaths` + `filterDepartmentPublications` on `department_ids`; `isPublicDepartment` + `isPublicPublication`; `PublicationItem` list rows link canonical research publication detail routes (no department duplicate detail URLs); build-time partition + optional client year facet when >1 year; `publicationPdfHref` honours PRD P4 gating stub. Follow-ons: optional `publications-index.json` per 05 §6.2 when corpus grows; wire department home publications teaser when content model adds featured block; consolidate department listing helpers under `src/lib/` if a fourth consumer appears. |
