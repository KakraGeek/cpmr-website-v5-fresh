---
/**
 * E4-S02 — History at `/about/history/` (`T_GENERIC_CONTENT` + milestone timeline; governed `src/content/pages/about/history.json`).
 */
import GenericContentPage from '../../../components/content/GenericContentPage.astro';
import { getGenericPageData } from '../../../components/content/generic-page-data';

const pathname = Astro.url.pathname;
const page = getGenericPageData('about/history');
---

<GenericContentPage page={page} pathname={pathname} seoCanonicalPath="/about/history/" />
