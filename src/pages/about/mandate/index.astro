---
/**
 * E4-S03 — Mandate at `/about/mandate/` (`T_GENERIC_CONTENT`; governed `src/content/pages/about/mandate.json`).
 */
import GenericContentPage from '../../../components/content/GenericContentPage.astro';
import { getGenericPageData } from '../../../components/content/generic-page-data';

const pathname = Astro.url.pathname;
const page = getGenericPageData('about/mandate');
---

<GenericContentPage page={page} pathname={pathname} seoCanonicalPath="/about/mandate/" />
